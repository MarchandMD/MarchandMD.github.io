steps:

1. I have all the divs in a NodeList
2. I now need to give them all an event listener for a click event, and a handler function
3. I now need to change the event to a keydown event, and specify which key to respond to...
4. If I pass in the event object, I'll have more to work with...
5. I pass the event object to the handler...
6. Since I'm listening for keydown events, I'm going to switch my eventListener for keydown onto the window object...
7. Now, I know which keys I'm e.keyCode I'm listening for.
8. Now, I have a console.log happening for the 4 keys I'm focused on. I need to add the audio files to the script, and play on keydown
I'll first try to add the elements in the JS
9. Ok, so the keys are associated with the sounds. However, the assignment included a couple of extras such as: allowing for clipping of the sound if I mash on a key and also some CSS styling. I'll add some animations perhaps. 